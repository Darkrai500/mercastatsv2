services:
  db:
    image: postgres:16-alpine
    container_name: mercastats_db
    restart: always
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations/0001_initial_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./backend/migrations/0002_ml_views.sql:/docker-entrypoint-initdb.d/02-ml-views.sql:ro
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/dockerfile
    container_name: mercastats_backend
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - BACKEND_HOST=0.0.0.0
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
      - INTELLIGENCE_SERVICE_URL=http://intelligence-service:8001
      - INTELLIGENCE_API_KEY=${INTELLIGENCE_API_KEY:-}
      - INTELLIGENCE_TIMEOUT_SECS=${INTELLIGENCE_TIMEOUT_SECS:-30}
      - INTELLIGENCE_MAX_RETRIES=${INTELLIGENCE_MAX_RETRIES:-2}
      - DEMO_USER_EMAIL=${DEMO_USER_EMAIL:-}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      db:
        condition: service_healthy
      intelligence-service:
        condition: service_started

  intelligence-service:
    build:
      context: ./intelligence-service
      dockerfile: Dockerfile
    container_name: mercastats_intelligence
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
      - INTELLIGENCE_API_KEY=${INTELLIGENCE_API_KEY:-}
    ports:
      - "${INTELLIGENCE_PORT:-8001}:8001"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    container_name: mercastats_frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      backend:
        condition: service_started

volumes:
  postgres_data:
